# File-encryption-using-C
A command line file encryption and decryption utility written in C using XOR based encryption, dynamic memory allocation, file handling, string manipulation, and password verification. The program supports encryption, decryption, encrypted file detection, three password attempts, and automatic file deletion after repeated incorrect passwords.
